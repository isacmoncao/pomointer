It should read a file.
For each line in the format '<string>: <sequence of stars>' it should print

<date>
<string>:
<'🍅' for each star> -> <time formatted as hours and minutes, or just hours or just minutes>

A POMO variable defines the pomodoro size. If it doesn't exist, defaults to 30 minutes.
A DATE variable defines the date of that register. If it doesn't exist, defaults to the modification
date of the file.

Variables can also be used as abbreviations.

----

1 - read the file
2 - for each line that contains ':' split it into two parts, first is subject name and second is pomodoros
3 - remove spaces from each side
4 - count the ammount of stars in the second string and store it somewhere
